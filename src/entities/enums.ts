export enum Role {
  ADMIN = "ADMIN",
  ANNOTATOR = "ANNOTATOR",
}

export enum AdType {
  COMMERCIAL_BREAK = "Ads",
  SPOT_OUTSIDE_BREAK = "SPOT_OUTSIDE_BREAK",
  PSA = "PSA",
}

export enum PromoType {
  TRAILER = "TRAILER",
  ANNOUNCEMENT = "ANNOUNCEMENT",
  TEASER = "TEASER",
  PROMO_SPOT = "PROMO_SPOT",
}
